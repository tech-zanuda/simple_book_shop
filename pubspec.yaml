name: simple_book_shop
description: "Простое приложение книжного магазина"

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  firebase_ui_localizations: ^1.2.0

  cupertino_icons: ^1.0.6
  firebase_core: ^2.31.0
  firebase_auth: ^4.19.5
  firebase_ui_auth: ^1.14.0
  flex_color_scheme: ^7.3.1
  google_fonts: ^6.2.1
  cloud_firestore: ^4.17.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
